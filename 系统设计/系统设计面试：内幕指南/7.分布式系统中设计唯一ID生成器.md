

## UUID

UUID 是一个 128 位数字，用于识别计算机系统中的信息。UUID 发生碰撞的概率非常低。

**优点：**

- 生成 UUID 很简单。服务器之间不需要协调，因此不会有任何同步问题。
- 该系统易于扩展，因为每个 Web 服务器负责生成它们使用的 ID。 ID 生成器可以轻松地与 Web 服务器一起扩展。


**缺点：**

- ID 是 128 位长，位数较多。
- ID 不会随时间上升
- ID 可以是非数字的。


## Ticket 服务器

其实就是一台单独的服务器，每次生成都会参考现有 web server 的情况来生成。

**优点：**

- 数字 ID
- 易于实施，适用于中小型应用程序


**缺点：**

- 单点故障。单个票务服务器意味着如果票务服务器发生故障，所有依赖它的系统都将面临问题。为了避免单点故障，我们可以设置多个票务服务器。然而，这将引入新的挑战，如数据同步。



## 雪花算法

每个部分进行解释：

- 符号位：1 位，它将始终为 0。这是为将来使用保留的。它可以潜在地用于区分有符号数和无符号数。
    
- 时间戳：41 位。自纪元或自定义纪元以来的毫秒数。我们使用 Twitter 雪花默认纪元 1288834974657，相当于 2010 年 11 月 4 日 01:42:54 UTC。
    
- 数据中心 ID：5 位，这给了我们 25=3225=32 个数据中心。
    
- 机器 ID：5 位，每个数据中心有 25=3225=32 台机器
    
- 序列号：12 位。对于在该机器/进程上生成的每个 ID，序列号都会递增 1。该数字每毫秒重置为 0。



## 公司正在使用的

雪花算法中主要是不太好识别，也就是无法一眼看出来具体信息。

日期位

```
260831
```


DCN: 这个其实可以参考雪花算法

```
0QD0
```


系统号，如果想要更直观可以直接使用系统名字，但是有个问题就是长度不能固定。

```
B2HR
```

后 18 位:时间戳位，而且本身有顺序

```
使用雪花算法+base32编码改造
```



同时采用了业务流水和系统流水两个流水来标识一笔交易请求，这样我们一笔交易如果包含多次请求既可以使用系统流水标识每次不同的调用，也可以使用业务流水标识一次交易。

































